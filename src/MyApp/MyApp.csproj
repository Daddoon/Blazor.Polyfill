<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net5.0</TargetFramework>
    <DefaultItemExcludes>${DefaultItemExcludes};node_modules\**</DefaultItemExcludes>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BlazorMobile.Build" Version="3.2.8" />
    <PackageReference Include="BlazorPolyfill.Build" Version="5.0.100.6" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Blazor.Polyfill.Server\Blazor.Polyfill.Server.csproj" />
  </ItemGroup>

  <Target Name="BuildES5ModulesPack" AfterTargets="GenerateBlazorPolyfillBuild">
    <Message Importance="High" Text="... comes Two." />
  </Target>


  <!--<Target Name="BuildES5ModulesPack" BeforeTargets="Build">
    <Exec Command="npm run build" />
  </Target>-->

</Project>
